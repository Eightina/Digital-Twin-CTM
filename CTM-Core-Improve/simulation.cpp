#include "simulation.h"


simulation::simulation(char* inputname) {
	std::cout << "simulation object" << std::endl;



}

void simulation::initial_control() {
	memset(omega, false, sizeof(omega));
	char str[256], _str[256];
	int istr = 0;


}
